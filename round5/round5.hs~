module Codewars.Kata.Next5 where
roundToNext5 :: Int -> Int
roundToNext5 n
  | lastDigit > 5  = (+) n $ abs $ mod n 5 - 5
  | lastDigit == 5 = n
  | otherwise      = (- ) n $ abs $ mod n 5
  where lastDigit = mod n 10
